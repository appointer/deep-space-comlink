{
  "websiteName": "{{ websiteName }}",
  "websitePushID": "{{ websitePushId }}",
  "allowedDomains": [
    "{{ host }}"
  ],
  "urlFormatString": "{{ host }}/%@",
  "authenticationToken": "{{ authenticationToken }}",
  "webServiceURL": "{{ host }}/dsc"
}
